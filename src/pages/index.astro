---
import Layout from '../layouts/Layout.astro';
import FeatureCard from '../components/FeatureCard.astro';
import UseCaseCard from '../components/UseCaseCard.astro';
import { Icon } from 'astro-icon/components';
---

<Layout title="Gillie AI - Your Intelligent Assistant" description="Gillie AI is an intelligent assistant that helps you with daily tasks, document processing, and much more.">

  <!-- ── HERO ──────────────────────────────────────────────────────────── -->
  <section class="relative pt-4 pb-16 md:pt-6 md:pb-20 overflow-hidden">
    <!-- Background decoration -->
    <div class="absolute inset-0 -z-10">
      <div class="absolute top-0 left-0 w-[700px] h-[700px] bg-gradient-to-br from-blue-500/15 to-transparent rounded-full blur-3xl animate-pulse-slow"></div>
      <div class="absolute bottom-0 right-0 w-[500px] h-[500px] bg-gradient-to-tr from-cyan-500/15 to-transparent rounded-full blur-3xl"></div>
    </div>

    <div class="container mx-auto px-6 max-w-7xl">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-center">
        <!-- Text -->
        <div class="reveal text-center lg:text-left">
          <h1 class="text-4xl sm:text-5xl md:text-6xl font-bold text-white mb-5 md:mb-6 leading-tight tracking-tight">
            Your Personal
            <span class="gradient-text"> Intelligent</span>
            <br/>Assistant
          </h1>
          <p class="text-base sm:text-lg md:text-xl text-white/60 mb-7 md:mb-10 leading-relaxed max-w-lg mx-auto lg:mx-0">
            Gillie AI is more than a chatbot. It processes documents, automates tasks, and integrates with your favorite tools.
          </p>

          <div class="flex flex-col sm:flex-row gap-4 mb-8 md:mb-14 justify-center lg:justify-start">
            <a href="#contact" class="btn-primary justify-center text-base px-8 py-4 shadow-[0_4px_20px_0_rgba(30,100,220,0.5)] hover:shadow-[0_6px_28px_0_rgba(30,100,220,0.6)] hover:scale-[1.02]">
              <Icon name="lucide:rocket" class="w-5 h-5" />
              Get Started Free
            </a>
            <a href="#features" class="btn-secondary justify-center text-base px-8 py-4 hover:scale-[1.02]">
              <Icon name="lucide:play-circle" class="w-5 h-5" />
              See Features
            </a>
          </div>

          <!-- Stats row -->
          <div class="flex flex-wrap items-center gap-6 md:gap-8 text-sm justify-center lg:justify-start">
            <div class="flex items-center gap-2.5 bg-white/[0.04] px-4 py-2.5 rounded-full border border-white/10">
              <div class="w-8 h-8 rounded-full bg-green-500/10 flex items-center justify-center">
                <Icon name="lucide:shield-check" class="w-4 h-4 text-green-400" />
              </div>
              <span class="text-white/70 font-medium">No credit card required</span>
            </div>
            <div class="flex items-center gap-2.5 bg-white/[0.04] px-4 py-2.5 rounded-full border border-white/10">
              <div class="w-8 h-8 rounded-full bg-yellow-500/10 flex items-center justify-center">
                <Icon name="lucide:zap" class="w-4 h-4 text-yellow-400" />
              </div>
              <span class="text-white/70 font-medium">Setup in minutes</span>
            </div>
          </div>
        </div>

        <!-- Illustration -->
        <div class="relative reveal reveal-delay-2 flex items-center justify-center hidden sm:flex">
          <div class="absolute inset-0 bg-gradient-to-br from-blue-500/20 via-cyan-500/10 to-transparent rounded-full blur-2xl"></div>
          <div class="relative z-10 animate-float w-full">
            <img
              src={`${import.meta.env.BASE_URL}assets/abyssal-fish.png`}
              alt="Gillie AI — bioluminescent abyssal fish"
              class="w-full max-w-xs sm:max-w-lg md:max-w-2xl mx-auto drop-shadow-2xl"
            />
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ── LOGOS / TRUST BAR ─────────────────────────────────────────────── -->
  <section class="py-10 border-y border-white/10 bg-gradient-to-b from-white/[0.02] to-white/[0.04]">
    <div class="container mx-auto px-6 max-w-6xl">
      <p class="text-center text-sm text-white/50 mb-8 uppercase tracking-widest font-medium">Compatible with the tools you already use</p>
      <div class="flex flex-wrap justify-center items-center gap-x-10 gap-y-4">
        {[
          { icon: 'lucide:bot',           label: 'OpenAI' },
          { icon: 'lucide:cpu',           label: 'Anthropic' },
          { icon: 'lucide:zap',           label: 'DeepSeek' },
          { icon: 'lucide:brain',         label: 'Ollama' },
          { icon: 'lucide:message-circle',label: 'WhatsApp' },
          { icon: 'lucide:send',          label: 'Telegram' },
          { icon: 'lucide:hash',          label: 'Slack' },
          { icon: 'lucide:calendar',      label: 'Google Calendar' },
          { icon: 'lucide:hard-drive',    label: 'Google Drive' },
          { icon: 'lucide:database',      label: 'SQL DB' },
          { icon: 'lucide:file-text',     label: 'PDF / OCR' },
          { icon: 'lucide:cloud',         label: 'AWS S3' },
        ].map(item => (
          <div class="flex items-center gap-2 text-white/70 hover:text-white font-semibold text-sm transition-colors duration-200">
            <Icon name={item.icon} class="w-4 h-4" /> {item.label}
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ── DEPLOYMENT MODES ─────────────────────────────────────────────── -->
  <section class="py-12 md:py-20">
    <div class="container mx-auto px-6 max-w-6xl">

      <div class="text-center mb-12 reveal">
        <span class="inline-flex items-center gap-2 text-gillie-blue text-sm font-semibold uppercase tracking-widest mb-4">
          <Icon name="lucide:monitor-smartphone" class="w-4 h-4" />
          Where It Runs
        </span>
        <h2 class="section-title">Your assistant, in any environment</h2>
        <p class="section-subtitle">
          Use it from your computer for daily tasks or deploy it on a server to automate your systems 24/7
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">

        <!-- PC / Local -->
        <div class="reveal reveal-delay-1 group relative bg-white/[0.03] rounded-lg border border-white/10 p-8 hover:bg-white/[0.05] hover:-translate-y-1 hover:shadow-[0_12px_40px_rgb(0,0,0,0.6)] transition-all duration-300 overflow-hidden cursor-pointer">
          <div class="absolute -bottom-8 -right-8 w-40 h-40 bg-blue-500/5 group-hover:bg-blue-500/10 rounded-full blur-2xl pointer-events-none transition-all duration-300"></div>

          <div class="inline-flex items-center justify-center w-12 h-12 rounded-lg bg-blue-500/10 mb-6 group-hover:scale-110 group-hover:bg-blue-500/20 transition-all duration-300">
            <Icon name="lucide:laptop" class="w-6 h-6 text-blue-400" />
          </div>

          <h3 class="text-xl font-bold text-white mb-3">On Your PC</h3>
          <p class="text-white/70 text-sm leading-relaxed mb-6">
            Personal assistant in your terminal or web interface. Helps you read files, search information, write code, and manage daily tasks without leaving your workflow.
          </p>

          <ul class="space-y-2.5">
            {[
              { icon: 'lucide:terminal',    text: 'Interactive CLI with history and modes (plan/auto/yolo)' },
              { icon: 'lucide:file-search', text: 'Read, edit, and create local files' },
              { icon: 'lucide:git-branch',  text: 'Native Git: status, diff, log, commit' },
              { icon: 'lucide:globe',       text: 'Automate browser (Chromium)' },
              { icon: 'lucide:database',    text: 'Query local SQL databases' },
              { icon: 'lucide:mic',         text: 'Voice dictation (STT) and spoken responses (TTS)' },
            ].map(item => (
              <li class="flex items-center gap-3 text-sm text-white/60">
                <div class="flex-shrink-0 w-7 h-7 rounded bg-blue-500/10 flex items-center justify-center">
                  <Icon name={item.icon} class="w-3.5 h-3.5 text-blue-400" />
                </div>
                {item.text}
              </li>
            ))}
          </ul>
        </div>

        <!-- VPS / Server -->
        <div class="reveal reveal-delay-2 group relative bg-white/[0.03] rounded-lg border border-white/10 p-8 hover:bg-white/[0.05] hover:-translate-y-1 hover:shadow-[0_12px_40px_rgb(0,0,0,0.6)] transition-all duration-300 overflow-hidden cursor-pointer">
          <div class="absolute -bottom-8 -right-8 w-40 h-40 bg-cyan-500/10 group-hover:bg-cyan-500/15 rounded-full blur-2xl pointer-events-none transition-all duration-300"></div>

          <div class="inline-flex items-center justify-center w-12 h-12 rounded-lg bg-cyan-500/10 mb-6 group-hover:scale-110 group-hover:bg-cyan-500/20 transition-all duration-300">
            <Icon name="lucide:server" class="w-6 h-6 text-cyan-400" />
          </div>

          <h3 class="text-xl font-bold text-white mb-3">On Your VPS</h3>
          <p class="text-white/70 text-sm leading-relaxed mb-6">
            Deploy it on your server to work autonomously. Receive messages via WhatsApp or Telegram, run scheduled tasks, and automate your systems without manual intervention.
          </p>

          <ul class="space-y-2.5">
            {[
              { icon: 'lucide:message-circle', text: 'Handle WhatsApp, Telegram, and Slack' },
              { icon: 'lucide:clock',          text: 'Cron scheduler: 24/7 tasks' },
              { icon: 'lucide:calendar',       text: 'Sync Google Calendar and Drive' },
              { icon: 'lucide:database',       text: 'Query and operate SQL databases' },
              { icon: 'lucide:webhook',        text: 'Incoming webhooks and REST API' },
              { icon: 'lucide:cloud',          text: 'Cloud storage (S3, Drive)' },
            ].map(item => (
              <li class="flex items-center gap-3 text-sm text-white/60">
                <div class="flex-shrink-0 w-7 h-7 rounded bg-white/[0.05] flex items-center justify-center">
                  <Icon name={item.icon} class="w-3.5 h-3.5 text-cyan-400" />
                </div>
                {item.text}
              </li>
            ))}
          </ul>
        </div>

      </div>
    </div>
  </section>

  <!-- ── FEATURES ─────────────────────────────────────────────────────── -->
  <section
    id="features"
    class="py-12 md:py-20 relative overflow-hidden"
    style={`background-image: url('${import.meta.env.BASE_URL}assets/gillie-ocean.png'); background-size: cover; background-position: center 40%;`}
  >
    <div class="absolute inset-0 bg-gradient-to-b from-black/60 via-black/50 to-black/60"></div>

    <div class="relative z-10 container mx-auto px-6 max-w-6xl">
      <div class="text-center mb-12 reveal">
        <span class="inline-flex items-center gap-2 text-gillie-blue text-sm font-semibold uppercase tracking-widest mb-4">
          <Icon name="lucide:layers" class="w-4 h-4" />
          Capabilities
        </span>
        <h2 class="section-title">Key Features</h2>
        <p class="section-subtitle">
          Discover everything Gillie AI can do for you
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <div class="reveal reveal-delay-1">
          <FeatureCard
            icon="lucide:brain"
            title="AI Document Processing"
            description="Read, analyze, and extract data from PDFs, Word, Excel, and images with OCR. Also write and edit files directly."
            gradient="from-violet-500 to-purple-600"
          />
        </div>
        <div class="reveal reveal-delay-2">
          <FeatureCard
            icon="lucide:search"
            title="RAG Over Your Data"
            description="Index your own documents and get accurate answers based on your knowledge. Vector search with FAISS and Qdrant."
            gradient="from-blue-500 to-cyan-500"
          />
        </div>
        <div class="reveal reveal-delay-3">
          <FeatureCard
            icon="lucide:memory-stick"
            title="Persistent Memory"
            description="The assistant remembers information between sessions: preferences, previous solutions, and accumulated context of your work."
            gradient="from-cyan-500 to-teal-500"
          />
        </div>
        <div class="reveal reveal-delay-4">
          <FeatureCard
            icon="lucide:clock"
            title="Scheduled Tasks"
            description="Create and manage automated tasks with cron. The assistant runs workflows without manual intervention on the schedules you define."
            gradient="from-green-500 to-emerald-500"
          />
        </div>
        <div class="reveal reveal-delay-5">
          <FeatureCard
            icon="lucide:globe"
            title="Autonomous Web Navigation"
            description="Control the browser autonomously: navigate, click, extract content, and take screenshots of any website."
            gradient="from-orange-500 to-amber-500"
          />
        </div>
        <div class="reveal reveal-delay-6">
          <FeatureCard
            icon="lucide:git-branch"
            title="Development Workflow"
            description="Read repos, run git status, diff, log, and commits. Execute shell commands and analyze your code directly from chat."
            gradient="from-pink-500 to-rose-500"
          />
        </div>
      </div>
    </div>
  </section>

  <!-- ── HOW IT WORKS ──────────────────────────────────────────────────── -->
  <section id="how-it-works" class="py-12 md:py-20">
    <div class="container mx-auto px-6 max-w-6xl">
      <div class="text-center mb-12 reveal">
        <span class="inline-flex items-center gap-2 text-gillie-blue text-sm font-semibold uppercase tracking-widest mb-4">
          <Icon name="lucide:map" class="w-4 h-4" />
          Process
        </span>
        <h2 class="section-title">How It Works</h2>
        <p class="section-subtitle">
          From zero to your assistant running in minutes
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 relative">
        <!-- Connecting line (desktop) -->
        <div class="hidden md:block absolute top-8 left-[calc(33%+2rem)] right-[calc(33%+2rem)] h-px bg-gradient-to-r from-blue-500/30 via-cyan-500/30 to-teal-500/30"></div>

        <!-- Step 1 -->
        <div class="group bg-white/[0.03] rounded-lg border border-white/10 p-7 hover:bg-white/[0.05] hover:-translate-y-1 hover:shadow-[0_8px_30px_rgb(0,0,0,0.5)] hover:border-blue-500/30 transition-all duration-300 reveal reveal-delay-1 cursor-pointer">
          <div class="flex items-center gap-4 mb-5">
            <div class="flex-shrink-0 inline-flex items-center justify-center w-14 h-14 rounded-lg bg-gradient-to-br from-blue-500 to-blue-600 text-white group-hover:scale-110 transition-transform duration-300">
              <Icon name="lucide:package" class="w-7 h-7" />
            </div>
            <div>
              <div class="text-xs font-bold text-blue-400 uppercase tracking-widest">Step 01</div>
              <h3 class="text-lg font-bold text-white">Installation</h3>
            </div>
          </div>
          <p class="text-white/60 text-sm leading-relaxed mb-5">
            Start the server with Docker in a single command. You can also install it locally with pip to use the CLI.
          </p>
          <!-- Quick start snippet -->
          <div class="bg-white/[0.05] rounded px-4 py-3 font-mono text-xs text-white/60 space-y-1">
            <div><span class="text-white/35"># Docker (server)</span></div>
            <div><span class="text-cyan-400">make</span> build <span class="text-white/35">&amp;&amp;</span> <span class="text-cyan-400">make</span> run</div>
            <div class="pt-1"><span class="text-white/35"># or local CLI</span></div>
            <div><span class="text-cyan-400">pip</span> install gillie-ai</div>
          </div>
        </div>

        <!-- Step 2 -->
        <div class="group bg-white/[0.03] rounded-lg border border-white/10 p-7 hover:bg-white/[0.05] hover:-translate-y-1 hover:shadow-[0_8px_30px_rgb(0,0,0,0.5)] hover:border-cyan-500/30 transition-all duration-300 reveal reveal-delay-2 cursor-pointer">
          <div class="flex items-center gap-4 mb-5">
            <div class="flex-shrink-0 inline-flex items-center justify-center w-14 h-14 rounded-lg bg-gradient-to-br from-cyan-500 to-blue-500 text-white group-hover:scale-110 transition-transform duration-300">
              <Icon name="lucide:settings-2" class="w-7 h-7" />
            </div>
            <div>
              <div class="text-xs font-bold text-cyan-400 uppercase tracking-widest">Step 02</div>
              <h3 class="text-lg font-bold text-white">Configuration</h3>
            </div>
          </div>
          <p class="text-white/60 text-sm leading-relaxed mb-5">
            Define your LLM provider and enable the integrations you need from the panel or the <code class="bg-white/10 px-1.5 py-0.5 rounded text-xs text-white/70">.env</code> file.
          </p>
          <ul class="space-y-2">
            {[
              { icon: 'lucide:key',           text: 'API key for OpenAI, Anthropic, DeepSeek, or local Ollama' },
              { icon: 'lucide:message-circle', text: 'Connect WhatsApp, Telegram, or Slack' },
              { icon: 'lucide:database',       text: 'Add SQL databases or RAG documents' },
              { icon: 'lucide:clock',          text: 'Configure automated cron tasks' },
            ].map(item => (
              <li class="flex items-center gap-2.5 text-sm text-white/60">
                <Icon name={item.icon} class="w-3.5 h-3.5 text-cyan-400 flex-shrink-0" />
                {item.text}
              </li>
            ))}
          </ul>
        </div>

        <!-- Step 3 -->
        <div class="group bg-white/[0.03] rounded-lg border border-white/10 p-7 hover:bg-white/[0.05] hover:-translate-y-1 hover:shadow-[0_8px_30px_rgb(0,0,0,0.5)] hover:border-teal-500/30 transition-all duration-300 reveal reveal-delay-3 cursor-pointer">
          <div class="flex items-center gap-4 mb-5">
            <div class="flex-shrink-0 inline-flex items-center justify-center w-14 h-14 rounded-lg bg-gradient-to-br from-teal-500 to-cyan-400 text-white group-hover:scale-110 transition-transform duration-300">
              <Icon name="lucide:play" class="w-7 h-7" />
            </div>
            <div>
              <div class="text-xs font-bold text-teal-400 uppercase tracking-widest">Step 03</div>
              <h3 class="text-lg font-bold text-white">Get to Work</h3>
            </div>
          </div>
          <p class="text-white/60 text-sm leading-relaxed mb-5">
            Interact from your preferred channel. The assistant remembers context, executes tools, and learns from your workflow.
          </p>
          <div class="grid grid-cols-2 gap-2">
            {[
              { icon: 'lucide:terminal',       label: 'CLI' },
              { icon: 'lucide:globe',          label: 'Web UI' },
              { icon: 'lucide:message-circle', label: 'WhatsApp' },
              { icon: 'lucide:send',           label: 'Telegram' },
              { icon: 'lucide:hash',           label: 'Slack' },
              { icon: 'lucide:code-2',         label: 'REST API' },
            ].map(item => (
              <div class="flex items-center gap-2 bg-white/[0.05] rounded px-3 py-2 text-xs font-medium text-white/60">
                <Icon name={item.icon} class="w-3.5 h-3.5 text-teal-400" />
                {item.label}
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ── USE CASES ─────────────────────────────────────────────────────── -->
  <section id="use-cases" class="py-12 md:py-20">
    <div class="container mx-auto px-6 max-w-6xl">
      <div class="text-center mb-12 reveal">
        <span class="inline-flex items-center gap-2 text-gillie-blue text-sm font-semibold uppercase tracking-widest mb-4">
          <Icon name="lucide:briefcase" class="w-4 h-4" />
          Applications
        </span>
        <h2 class="section-title">Use Cases</h2>
        <p class="section-subtitle">
          Gillie AI adapts to different needs and sectors
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <UseCaseCard
          title="Education"
          description="Assistant for students and teachers, educational material processing, personalized tutoring."
          features={["Process academic PDFs", "Answer specific questions", "Generate summaries"]}
          color="blue"
        />
        <UseCaseCard
          title="Business"
          description="Process automation, commercial document analysis, intelligent customer service."
          features={["Process contracts", "Analyze reports", "WhatsApp integration"]}
          color="green"
        />
        <UseCaseCard
          title="Development"
          description="API for developers, integration with existing applications, workflow automation."
          features={["RESTful API", "Webhooks", "Available SDKs"]}
          color="purple"
        />
        <UseCaseCard
          title="Personal"
          description="Personal organization, intelligent reminders, personal document processing."
          features={["Task management", "Reminders", "OCR processing"]}
          color="orange"
        />
      </div>
    </div>
  </section>

  <!-- ── API SECTION ───────────────────────────────────────────────────── -->
  <section id="api" class="py-12 md:py-20 relative overflow-hidden border-y border-white/10">
    <!-- Decorative glow -->
    <div class="absolute top-0 right-0 w-96 h-96 bg-blue-600/10 rounded-full blur-3xl"></div>
    <div class="absolute bottom-0 left-0 w-96 h-96 bg-cyan-600/10 rounded-full blur-3xl"></div>

    <div class="relative z-10 container mx-auto px-6 max-w-6xl">
      <div class="text-center mb-12 reveal">
        <span class="inline-flex items-center gap-2 text-cyan-400 text-sm font-semibold uppercase tracking-widest mb-4">
          <Icon name="lucide:terminal" class="w-4 h-4" />
          Developers
        </span>
        <h2 class="text-4xl md:text-5xl font-bold mb-4 tracking-tight text-white">Powerful API</h2>
        <p class="text-lg text-white/60 max-w-2xl mx-auto">
          Integrate Gillie AI into your applications with our complete and documented API
        </p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-start">
        <!-- Features list -->
        <div class="reveal reveal-delay-1 space-y-4">
          {[
            { icon: 'lucide:book-open',   label: 'OpenAPI / Swagger documentation' },
            { icon: 'lucide:shield',      label: 'Secure JWT authentication' },
            { icon: 'lucide:webhook',     label: 'Webhooks for real-time events' },
            { icon: 'lucide:layers',      label: 'SDKs for multiple languages' },
          ].map(item => (
            <div class="flex items-center gap-4 bg-white/[0.03] border border-white/10 rounded px-5 py-4 hover:bg-white/[0.05] transition-colors">
              <div class="flex-shrink-0 w-9 h-9 rounded bg-cyan-500/15 flex items-center justify-center">
                <Icon name={item.icon} class="w-5 h-5 text-cyan-400" />
              </div>
              <span class="text-white/80 font-medium">{item.label}</span>
            </div>
          ))}

          <div class="pt-2">
            <a href="#" class="btn-primary bg-gradient-to-r from-gillie-blue to-cyan-500 border-0">
              <Icon name="lucide:external-link" class="w-5 h-5" />
              View API Documentation
            </a>
          </div>
        </div>

        <!-- Code block -->
        <div class="reveal reveal-delay-2 bg-white/[0.03] rounded overflow-hidden border border-white/10 shadow-2xl">
          <!-- Window chrome -->
          <div class="flex items-center gap-2 px-5 py-3 border-b border-white/10 bg-white/[0.03]">
            <div class="w-3 h-3 rounded-full bg-red-500/80"></div>
            <div class="w-3 h-3 rounded-full bg-yellow-500/80"></div>
            <div class="w-3 h-3 rounded-full bg-green-500/80"></div>
            <span class="ml-3 text-white/35 text-xs font-mono">api.gillie.ai · example.js</span>
          </div>
          <pre class="text-sm text-white/60 p-6 overflow-x-auto leading-relaxed font-mono">{`// API usage example
const response = await fetch(
  'https://api.gillie.ai/v1/chat',
  {
    method: 'POST',
    headers: {
      'Authorization': 'Bearer YOUR_API_KEY',
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      message: 'Process this PDF document',
      document_url: 'https://example.com/doc.pdf',
      context: 'education'
    })
  }
);

const { response: reply } = await response.json();
console.log(reply);`}</pre>
        </div>
      </div>
    </div>
  </section>

  <!-- ── INTEGRATIONS ─────────────────────────────────────────────────── -->
  <section id="integrations" class="py-12 md:py-20 bg-gradient-to-b from-transparent via-white/[0.02] to-transparent">
    <div class="container mx-auto px-6 max-w-6xl">

      <div class="text-center mb-12 reveal">
        <span class="inline-flex items-center gap-2 text-gillie-blue text-sm font-semibold uppercase tracking-widest mb-4">
          <Icon name="lucide:plug" class="w-4 h-4" />
          Integrations
        </span>
        <h2 class="section-title">Connect with your entire stack</h2>
        <p class="section-subtitle">
          Gillie AI natively connects with the platforms you already use. No extra code.
        </p>
      </div>

      <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4">
        {[
          { icon: 'lucide:bot',           name: 'OpenAI',           desc: 'GPT-4o and advanced models',       color: 'bg-green-500/10 text-green-400' },
          { icon: 'lucide:cpu',           name: 'Anthropic',        desc: 'Claude Sonnet / Opus',             color: 'bg-orange-500/10 text-orange-400' },
          { icon: 'lucide:zap',           name: 'DeepSeek',         desc: 'Fast open source models',          color: 'bg-blue-500/10 text-blue-400' },
          { icon: 'lucide:share-2',       name: 'OpenRouter',       desc: 'Access to 100+ LLM models',        color: 'bg-purple-500/10 text-purple-400' },
          { icon: 'lucide:brain',         name: 'Ollama',           desc: 'Local models: Llama, Mistral…',   color: 'bg-indigo-500/10 text-indigo-400' },
          { icon: 'lucide:message-circle',name: 'WhatsApp',         desc: 'Meta Business API + Neonize Web',  color: 'bg-emerald-500/10 text-emerald-400' },
          { icon: 'lucide:send',          name: 'Telegram',         desc: 'Bot API with webhook',             color: 'bg-sky-500/10 text-sky-400' },
          { icon: 'lucide:hash',          name: 'Slack',            desc: 'Bot and slash commands',           color: 'bg-rose-500/10 text-rose-400' },
          { icon: 'lucide:calendar',      name: 'Google Calendar',  desc: 'Events and reminders OAuth',       color: 'bg-blue-500/10 text-blue-400' },
          { icon: 'lucide:hard-drive',    name: 'Google Drive',     desc: 'Read and upload files OAuth',      color: 'bg-yellow-500/10 text-yellow-400' },
          { icon: 'lucide:database',      name: 'SQL Databases',    desc: 'PostgreSQL, MySQL, SQLite…',       color: 'bg-slate-500/10 text-slate-400' },
          { icon: 'lucide:cloud',         name: 'AWS S3',           desc: 'File storage',                     color: 'bg-orange-500/10 text-orange-400' },
        ].map((item, i) => (
          <div class={`reveal reveal-delay-${(i % 6) + 1} group bg-white/[0.03] rounded border border-white/10 p-5 hover:bg-white/[0.05] transition-colors`}>
            <div class={`inline-flex items-center justify-center w-10 h-10 rounded ${item.color.split(' ')[0]} mb-3`}>
              <Icon name={item.icon} class={`w-5 h-5 ${item.color.split(' ')[1]}`} />
            </div>
            <div class="font-bold text-white text-sm mb-0.5">{item.name}</div>
            <div class="text-white/40 text-xs leading-relaxed">{item.desc}</div>
          </div>
        ))}
      </div>

      <!-- MCP note -->
      <p class="text-center text-sm text-white/40 mt-10 reveal">
        <Icon name="lucide:puzzle" class="w-4 h-4 inline mr-1.5 text-gillie-blue" />
        Compatible with <span class="font-semibold text-white/60">Model Context Protocol (MCP)</span> — connect any external tool in minutes
      </p>

    </div>
  </section>

  <!-- ── LLM PROVIDERS ─────────────────────────────────────────────────── -->
  <section class="py-16 border-y border-white/10 bg-gradient-to-r from-blue-500/5 via-transparent to-cyan-500/5">
    <div class="container mx-auto px-6 max-w-5xl">
      <div class="rounded-lg border border-white/10 p-10 bg-white/[0.02] backdrop-blur-sm">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-10 items-center">
          <div class="reveal">
            <span class="inline-flex items-center gap-2 text-gillie-blue text-sm font-semibold uppercase tracking-widest mb-4">
              <Icon name="lucide:cpu" class="w-4 h-4" />
              AI Models
            </span>
            <h3 class="text-3xl font-bold text-white mb-3 tracking-tight">No vendor lock-in</h3>
            <p class="text-white/60 text-sm leading-relaxed mb-6">
              Choose the model that best fits your use case and budget. Cloud APIs or local models with Ollama. Switch providers in seconds from the config, without touching code.
            </p>
            <div class="flex flex-wrap gap-2">
              {['GPT-4o', 'Claude Sonnet', 'DeepSeek R1', 'Gemini', 'Llama 3', 'Ollama', 'OpenRouter'].map(m => (
                <span class="bg-white/10 text-white/70 text-xs font-semibold px-3 py-1.5 rounded-full">{m}</span>
              ))}
            </div>
          </div>
          <div class="reveal reveal-delay-2 grid grid-cols-2 gap-3">
            {[
              { icon: 'lucide:eye',        title: 'Vision',       desc: 'Analyze images with dedicated vision models' },
              { icon: 'lucide:mic',        title: 'Voice',        desc: 'STT and TTS with multiple audio engines' },
              { icon: 'lucide:layers',     title: 'Embeddings',   desc: 'OpenAI, HuggingFace, or local models' },
              { icon: 'lucide:activity',   title: 'Traceability', desc: 'LangSmith for execution monitoring and debug' },
            ].map(item => (
              <div class="bg-white/[0.03] rounded border border-white/10 p-4 hover:bg-white/[0.05] transition-colors">
                <Icon name={item.icon} class="w-5 h-5 text-gillie-blue mb-2" />
                <div class="font-bold text-white text-sm mb-1">{item.title}</div>
                <div class="text-white/40 text-xs leading-relaxed">{item.desc}</div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ── PRICING ──────────────────────────────────────────────────────── -->
  <section id="pricing" class="py-12 md:py-20 relative overflow-hidden">
    <!-- Background decoration -->
    <div class="absolute inset-0 -z-10">
      <div class="absolute top-0 right-0 w-[600px] h-[600px] bg-gradient-to-bl from-blue-500/10 to-transparent rounded-full blur-3xl"></div>
      <div class="absolute bottom-0 left-0 w-[600px] h-[600px] bg-gradient-to-tr from-cyan-500/10 to-transparent rounded-full blur-3xl"></div>
    </div>
    <div class="container mx-auto px-6 max-w-6xl">

      <div class="text-center mb-12 reveal">
        <h2 class="section-title">Start free. Upgrade when it matters.</h2>
        <p class="section-subtitle">Core features are free forever. Paid plans add support, premium features, and team capabilities.</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-5 max-w-5xl mx-auto">

        <!-- Personal (free) -->
        <div class="reveal reveal-delay-1 group bg-white/[0.03] rounded-lg border border-gillie-blue/30 p-7 flex flex-col hover:bg-white/[0.05] hover:-translate-y-1 hover:shadow-[0_8px_30px_rgb(0,0,0,0.5)] hover:border-gillie-blue/40 transition-all duration-300">
          <p class="mb-3 text-xs font-bold uppercase tracking-[0.2em] text-gillie-blue">Always Free</p>
          <h3 class="mb-1 text-xl font-bold text-white">Personal</h3>
          <p class="mb-6 text-4xl font-bold text-white">$0</p>
          <div class="mb-6 space-y-2.5 text-sm text-white/70 flex-1">
            <p class="flex items-start gap-2"><Icon name="lucide:check" class="h-4 w-4 text-gillie-blue mt-0.5 shrink-0" />REST API + WebSocket</p>
            <p class="flex items-start gap-2"><Icon name="lucide:check" class="h-4 w-4 text-gillie-blue mt-0.5 shrink-0" />Document processing (PDF, Excel)</p>
            <p class="flex items-start gap-2"><Icon name="lucide:check" class="h-4 w-4 text-gillie-blue mt-0.5 shrink-0" />OpenAI, Anthropic, DeepSeek</p>
            <p class="flex items-start gap-2"><Icon name="lucide:check" class="h-4 w-4 text-gillie-blue mt-0.5 shrink-0" />Workspace memory</p>
            <p class="flex items-start gap-2"><Icon name="lucide:check" class="h-4 w-4 text-gillie-blue mt-0.5 shrink-0" />Docker · Ollama-native</p>
          </div>
          <a href="#contact" class="btn-primary text-center text-sm justify-center">
            Get Started Free
          </a>
        </div>

        <!-- Pro -->
        <div class="reveal reveal-delay-2 group bg-white/[0.03] rounded-lg border border-white/10 p-7 flex flex-col hover:bg-white/[0.05] hover:-translate-y-1 hover:shadow-[0_8px_30px_rgb(0,0,0,0.5)] hover:border-white/20 transition-all duration-300">
          <p class="mb-3 text-xs font-bold uppercase tracking-[0.2em] text-white/50">Pro</p>
          <h3 class="mb-1 text-xl font-bold text-white">Pro</h3>
          <p class="mb-6 text-4xl font-bold text-white">$5<span class="text-xl text-white/60"> / mo</span></p>
          <div class="mb-6 space-y-2.5 text-sm text-white/70 flex-1">
            <p class="flex items-start gap-2"><Icon name="lucide:check" class="h-4 w-4 text-green-400 mt-0.5 shrink-0" />Everything in Personal</p>
            <p class="flex items-start gap-2"><Icon name="lucide:check" class="h-4 w-4 text-green-400 mt-0.5 shrink-0" />WhatsApp integration</p>
            <p class="flex items-start gap-2"><Icon name="lucide:check" class="h-4 w-4 text-green-400 mt-0.5 shrink-0" />Priority feature requests</p>
            <p class="flex items-start gap-2"><Icon name="lucide:check" class="h-4 w-4 text-green-400 mt-0.5 shrink-0" />Faster fixes + premium support</p>
          </div>
          <button class="btn-secondary text-center text-sm justify-center" disabled>
            Join waitlist
          </button>
        </div>

        <!-- Team -->
        <div class="reveal reveal-delay-3 group bg-gillie-blue/5 rounded-lg border border-gillie-blue/20 p-7 flex flex-col hover:bg-gillie-blue/10 hover:-translate-y-1 hover:shadow-[0_8px_30px_rgb(30,100,220,0.3)] hover:border-gillie-blue/30 transition-all duration-300">
          <p class="mb-3 text-xs font-bold uppercase tracking-[0.2em] text-gillie-blue">Team</p>
          <h3 class="mb-1 text-xl font-bold text-white">Team</h3>
          <p class="mb-6 text-4xl font-bold text-white">$20<span class="text-xl text-white/60"> / seat</span></p>
          <div class="mb-6 space-y-2.5 text-sm text-white/70 flex-1">
            <p class="flex items-start gap-2"><Icon name="lucide:check" class="h-4 w-4 text-gillie-blue mt-0.5 shrink-0" />Everything in Pro</p>
            <p class="flex items-start gap-2"><Icon name="lucide:check" class="h-4 w-4 text-gillie-blue mt-0.5 shrink-0" />Private model routing</p>
            <p class="flex items-start gap-2"><Icon name="lucide:check" class="h-4 w-4 text-gillie-blue mt-0.5 shrink-0" />Dedicated deployments</p>
            <p class="flex items-start gap-2"><Icon name="lucide:check" class="h-4 w-4 text-gillie-blue mt-0.5 shrink-0" />Air-gap + enterprise support</p>
          </div>
          <button class="btn-secondary text-center text-sm justify-center" disabled>
            Talk to us
          </button>
        </div>

      </div>

      <p class="mt-8 text-center text-xs text-white/40 reveal reveal-delay-3">
        Paid tiers are in development. The free tier is complete and production-ready.
      </p>
    </div>
  </section>

  <!-- ── CTA ───────────────────────────────────────────────────────────── -->
  <section id="contact" class="py-12 md:py-20 relative overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-br from-gillie-blue via-blue-600 to-cyan-500 -z-10"></div>
    <!-- Noise texture overlay -->
    <div class="absolute inset-0 opacity-[0.03] -z-10" style="background-image: url('data:image/svg+xml,%3Csvg viewBox=%220 0 200 200%22 xmlns=%22http://www.w3.org/2000/svg%22%3E%3Cfilter id=%22n%22%3E%3CfeTurbulance type=%22fractalNoise%22 baseFrequency=%220.9%22/%3E%3C/filter%3E%3Crect width=%22100%25%22 height=%22100%25%22 filter=%22url(%23n)%22/%3E%3C/svg%3E');"></div>

    <div class="container mx-auto px-6 max-w-4xl">
      <div class="max-w-3xl mx-auto text-center reveal">
        <div class="inline-flex items-center gap-2 bg-white/15 text-white text-sm font-medium px-4 py-2 rounded-full mb-8">
          <Icon name="lucide:star" class="w-4 h-4" />
          Start today
        </div>
        <h2 class="text-4xl md:text-5xl font-bold mb-6 text-white tracking-tight">
          Ready to get started?
        </h2>
        <p class="text-xl mb-10 text-white/80 leading-relaxed">
          Join thousands of users already using Gillie AI to automate their tasks.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center mb-10">
          <a href="#" class="bg-white text-gillie-blue px-8 py-4 rounded font-bold hover:bg-gray-50 transition-all shadow-xl hover:shadow-2xl hover:-translate-y-px inline-flex items-center justify-center gap-2">
            <Icon name="lucide:rocket" class="w-5 h-5" />
            Get Started Free
          </a>
          <a href="#" class="border border-white/40 text-white px-8 py-4 rounded font-bold hover:bg-white/10 hover:border-white/60 transition-all inline-flex items-center justify-center gap-2">
            <Icon name="lucide:calendar" class="w-5 h-5" />
            Request Demo
          </a>
        </div>
        <div class="flex flex-wrap justify-center gap-6 text-sm text-white/70">
          <div class="flex items-center gap-1.5">
            <Icon name="lucide:check-circle" class="w-4 h-4 text-white/90" />
            No credit card required
          </div>
          <div class="flex items-center gap-1.5">
            <Icon name="lucide:check-circle" class="w-4 h-4 text-white/90" />
            14 days free
          </div>
          <div class="flex items-center gap-1.5">
            <Icon name="lucide:check-circle" class="w-4 h-4 text-white/90" />
            Support included
          </div>
        </div>
      </div>
    </div>
  </section>

</Layout>

<!-- Scroll reveal script -->
<script is:inline>
  const observer = new IntersectionObserver(
    (entries) => entries.forEach(e => {
      if (e.isIntersecting) {
        e.target.classList.add('visible');
        observer.unobserve(e.target);
      }
    }),
    { threshold: 0.12 }
  );
  document.querySelectorAll('.reveal').forEach(el => observer.observe(el));
</script>
