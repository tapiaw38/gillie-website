---
import { Icon } from 'astro-icon/components';

export interface Props {
  title: string;
  description: string;
  features: string[];
  color: 'blue' | 'green' | 'purple' | 'orange';
  icon?: string;
}

const { title, description, features, color = 'blue', icon } = Astro.props;

const config = {
  blue:   { accent: 'text-blue-400',   iconBg: 'bg-blue-500/10',   icon: icon ?? 'lucide:graduation-cap' },
  green:  { accent: 'text-emerald-400', iconBg: 'bg-emerald-500/10', icon: icon ?? 'lucide:briefcase' },
  purple: { accent: 'text-violet-400',  iconBg: 'bg-violet-500/10',  icon: icon ?? 'lucide:code-2' },
  orange: { accent: 'text-orange-400',  iconBg: 'bg-orange-500/10',  icon: icon ?? 'lucide:user' },
};

const c = config[color];
---

<div class="group bg-white/[0.03] rounded border border-white/10 p-7 hover:bg-white/[0.05] hover:-translate-y-0.5 transition-all duration-300">

  <!-- Icon -->
  <div class={`inline-flex items-center justify-center w-10 h-10 rounded ${c.iconBg} mb-5`}>
    <Icon name={c.icon} class={`w-5 h-5 ${c.accent}`} />
  </div>

  <h3 class="text-lg font-bold text-white mb-2">{title}</h3>
  <p class="text-white/60 text-sm leading-relaxed mb-5">{description}</p>

  <!-- Feature list -->
  <ul class="space-y-2">
    {features.map((feature) => (
      <li class="flex items-center gap-2.5 text-sm text-white/60">
        <Icon name="lucide:check" class={`w-4 h-4 flex-shrink-0 ${c.accent}`} />
        {feature}
      </li>
    ))}
  </ul>
</div>
